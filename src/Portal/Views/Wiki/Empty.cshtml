@model object

@{
    var cls = (Model is string) ? Model : string.Empty;
}

<p class="empty-block @cls">
    <span class="txt">TXT</span>
    <span class="msg">
        @Model.W("NoContentLine1")
        <br />
        @Model.W("NoContentLine2")
        <a class="contrib-link" href="#">@Model.W("NoContentBtn")</a>
    </span>
</p>
