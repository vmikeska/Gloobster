
@using System.Threading.Tasks
@using Gloobster.Portal.ViewModels
@model WikiModelBase

<div class="links wrapper">
    <h3>@Model.W("RelatedLinks")</h3>

    @foreach (var link in Model.GetRelatedLinks())
    {
        <a class="lbtn" href="@link.Link">@link.Name</a>
    }
</div>