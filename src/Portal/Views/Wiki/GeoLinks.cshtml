
@using System.Threading.Tasks
@using Gloobster.Portal.ViewModels
@model WikiModelBase

<h3>Related links</h3>
<div class="tags">
    @foreach (var link in Model.GetRelatedLinks())
    {
        <a class="tag" href="@link.Link">@link.Name</a>
    }
</div>