
@using System.Threading.Tasks
@using Gloobster.Portal.ViewModels
@model NewWikiModelBase

@if (Model.IsAdmin)
{    
    @Html.Partial("AAAdminTemplates")

    <div class="wrapper admin">
        <input id="adminMode" type="checkbox"><label for="adminMode">Admin mode</label>        
    </div>
}