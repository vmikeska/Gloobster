namespace Gloobster.Enums
{
    public enum ArticleType { Continent, Country, City }

    public enum ReligionType { NA, RomanCatolic, Hinduism, Islam }
    
    public enum Continent { Europe, Australia, Africa, Antarctica, SouthAmerica, NorthAmerica, Asia }

    public enum LinkType { Facebook, WIKI, Geonames }

    public enum EventType { Delete, Create, Update }
}